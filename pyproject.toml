[project]
name = "homeassistant-ads-custom"
version = "1.1.5"
description = "Home Assistant custom integration for Beckhoff ADS"
requires-python = ">=3.12"

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"

[tool.ruff]
target-version = "py312"
